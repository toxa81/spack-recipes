spack:
  specs:
    - sirius@develop %gcc@11.4.0 +cuda +fortran +scalapack ^intel-oneapi-mkl+cluster ^cray-mpich ^umpire+cuda~device_alloc ^cuda@11.8
  view: false
  concretizer:
    unify: true
    reuse: true
